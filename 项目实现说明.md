# 项目实现说明

## 本次重构目标

这次已经推翻上次基于 DashScope SDK `Generation` 的底层写法，改为使用 Spring AI 的标准能力：

- `ChatModel dashscopeChatModel`
- `ChatClient`
- `MessageChatMemoryAdvisor`
- `ChatMemory`
- `ChatMemoryRepository`
- `ChatClient#entity(...)` 结构化输出

后续你继续做 RAG、Advisor、Tool Calling、MCP、VectorStore 时，可以沿着 Spring AI 的方式继续扩展。

## 1. Spring AI 依赖与项目升级

为了使用 Spring AI 1.0 和 Spring AI Alibaba DashScope，本项目已切换到：

- Spring Boot 3.4.4
- Java 21
- Spring AI 1.0.0
- Spring AI Alibaba 1.0.0.2

同时做了必要兼容迁移：

- `javax.*` 改为 `jakarta.*`
- MyBatis Plus 改为 Boot3 starter
- Sa-Token 改为 Boot3 starter
- Knife4j 改为 Jakarta OpenAPI3 starter

## 2. 课程文件 AI 审核

`CourseServiceImpl#uploadCourseFile` 中原来的两个 TODO 已完成。

流程如下：

1. 老师或管理员上传课程文件。
2. 系统先把文件转换成 Markdown。
3. 使用 Spring AI `ChatClient#entity(...)` 调用 DashScope 模型进行结构化审核。
4. 审核不通过时，直接拒绝上传，不会写入 COS 和数据库。
5. 审核通过后，上传原文件和 Markdown 文件。
6. 如果用户选择章节解析，则提取大章节并写入 `chapter` 表。

章节只切割大章节，不做过细知识点拆分。

## 3. 文件转 Markdown

保留了独立的文档转换服务：

- `DocumentMarkdownService`
- `TikaDocumentMarkdownServiceImpl`

现在先用 Tika 支持 PDF、Word、PPT、TXT、Markdown 等文件抽取文本并转成 Markdown 风格文本。
后续如果要接 Spring AI 的 DocumentReader、ETL Pipeline、VectorStore，可以继续替换这一层，不影响课程上传主流程。

## 4. Spring AI 记忆

新增数据库持久化记忆：

- `DatabaseChatMemoryRepository implements ChatMemoryRepository`
- `ChatMemoryMessage`
- `ChatMemoryMessageMapper`
- `chat_memory_message` 表

保存字段包括：

- 用户 id
- 对话 id
- 消息角色
- 消息内容
- 消息顺序
- 创建时间

新增文件持久化记忆：

- `FileBasedChatMemory implements ChatMemory`

默认保存到：

- `data/ai-memory`

`data/` 已加入 `.gitignore`。

## 5. 智能体结构

当前结构按后续扩展划分：

- `studyaiagent/app`：应用编排层，例如 `StudyApp`
- `studyaiagent/course`：课程文件审核、章节解析、Markdown 存储
- `studyaiagent/memory`：Spring AI 记忆实现
- `ai/service`：通用 Spring AI 调用和文档转换能力
- `config`：Spring AI、ChatMemory、后续 RAG / Tool / MCP 配置预留

`StudyApp` 现在已经按示例项目方式使用 `ChatClient`，并通过 `MessageChatMemoryAdvisor` 接入数据库记忆。

## 6. 本地配置

已创建并更新：

- `src/main/resources/application-local.yml`

其中 COS 和 DashScope 配置只用于本地测试。
该文件已被 `.gitignore` 忽略，不会提交到 GitHub。

Spring AI 使用的配置结构是：

```yaml
spring:
  ai:
    dashscope:
      api-key: xxx
      chat:
        options:
          model: xxx
```

## 7. 数据库变更

`course_file` 表新增：

- `markdown_key`
- `markdown_url`

`chat_memory_message` 表新增：

- `message_order`

## 8. 测试结果

已执行：

```bash
.\mvnw.cmd test
```

测试通过，Spring Boot 3 + Spring AI 上下文可以正常启动。