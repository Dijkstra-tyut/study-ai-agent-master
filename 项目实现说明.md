# 项目实现说明

## 本次重构目标

项目已使用 Spring AI 的标准能力：

- `ChatModel dashscopeChatModel`
- `ChatClient`
- `MessageChatMemoryAdvisor`
- `ChatMemory`
- `ChatMemoryRepository`
- `ChatClient#entity(...)` 结构化输出

后续继续做 RAG、Advisor、Tool Calling、MCP、VectorStore 时，可以沿着 Spring AI 的方式扩展。

## 1. Spring AI 依赖与项目升级

为了使用 Spring AI 1.0 和 Spring AI Alibaba DashScope，本项目已切换到：

- Spring Boot 3.4.4
- Java 21
- Spring AI 1.0.0
- Spring AI Alibaba 1.0.0.2

同时做了必要兼容迁移：

- `javax.*` 改为 `jakarta.*`
- MyBatis Plus 改为 Boot3 starter
- Sa-Token 改为 Boot3 starter
- Knife4j 改为 Jakarta OpenAPI3 starter

## 2. 课程文件 AI 审核与章节切分

`CourseServiceImpl#uploadCourseFile` 中的流程由后端控制：

1. 老师或管理员上传课程文件。
2. 后端先把文件转换成 Markdown。
3. 后端调用 Spring AI 审核课程文件是否符合课程主题。
4. 审核不通过时直接返回错误，不上传 COS，不写数据库。
5. 审核通过后，上传原文件和 Markdown 文件到 COS，并写入课程文件表。
6. 后端判断 `needChapterAnalysis`。
7. 只有 `needChapterAnalysis == true` 时，才第二次调用大模型进行章节切分。
8. 如果无法从文件开头识别目录或大章节，直接返回“文件没有目录，无法进行章节切分”。

注意：是否进行章节切分由后端参数控制，不交给大模型判断。

## 3. 文件内容截取策略

无论是审核文件是否符合课程，还是切分章节，都只读取文件开头部分内容给大模型：

- 审核：读取前 8000 字符
- 章节切分：读取前 16000 字符

这样可以降低模型调用成本，也符合课程文件目录一般出现在前部的特点。

## 4. 文件转 Markdown

通用 AI 能力统一放在：

- `studyaiagent/core`

文档转换服务：

- `DocumentMarkdownService`
- `TikaDocumentMarkdownServiceImpl`

现在先用 Tika 支持 PDF、Word、PPT、TXT、Markdown 等文件抽取文本并转成 Markdown 风格文本。
后续如果要接 Spring AI 的 DocumentReader、ETL Pipeline、VectorStore，可以继续替换这一层。

## 5. Spring AI 记忆

数据库持久化记忆：

- `DatabaseChatMemoryRepository implements ChatMemoryRepository`
- `ChatMemoryMessage`
- `ChatMemoryMessageMapper`
- `chat_memory_message` 表

文件持久化记忆：

- `FileBasedChatMemory implements ChatMemory`

默认保存到：

- `data/ai-memory`

`data/` 已加入 `.gitignore`。

## 6. 当前文件结构

当前结构按职责划分：

- `studyaiagent/app`：应用编排层，例如 `StudyApp`、`ValidateCourseApp`
- `studyaiagent/core`：通用 AI 调用、文档转 Markdown
- `studyaiagent/course`：课程文件审核、章节解析、Markdown 存储
- `studyaiagent/memory`：Spring AI 记忆实现
- `config`：Spring AI、ChatMemory、后续 RAG / Tool / MCP 配置预留

## 7. 本地配置

本地配置文件：

- `src/main/resources/application-local.yml`

其中 COS 和 DashScope 配置只用于本地测试。
该文件已被 `.gitignore` 忽略，不会提交到 GitHub。

Spring AI 使用的配置结构是：

```yaml
spring:
  ai:
    dashscope:
      api-key: xxx
      chat:
        options:
          model: xxx
```

## 8. 数据库变更

`course_file` 表新增：

- `markdown_key`
- `markdown_url`

`chat_memory_message` 表新增：

- `message_order`

## 9. 测试结果

已执行：

```bash
.\mvnw.cmd test
```

测试通过，Spring Boot 3 + Spring AI 上下文可以正常启动。