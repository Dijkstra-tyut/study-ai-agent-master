# 项目实现说明

## 1. 当前项目能力

项目已经接入 Spring AI 和 Spring AI Alibaba DashScope，当前大模型调用都通过 Spring AI 的能力完成：

- `ChatModel dashscopeChatModel`
- `ChatClient`
- `MessageChatMemoryAdvisor`
- `ChatMemory`
- `ChatMemoryRepository`
- `ChatClient#entity(...)` 结构化输出

本地敏感配置放在 `src/main/resources/application-local.yml`，该文件已被 `.gitignore` 忽略，不会上传到 GitHub。

## 2. 课程文件处理

老师或管理员上传课程文件时，流程如下：

1. 后端先把文件转换为 Markdown。
2. 使用 Spring AI 审核课程文件是否符合课程主题。
3. 审核失败时直接返回错误，不上传 COS，不写数据库。
4. 审核通过后上传原文件和 Markdown 文件到 COS。
5. 如果 `needChapterAnalysis == true`，后端再次调用大模型只切分大章节。
6. 如果文件开头无法识别目录或大章节，直接返回“文件没有目录，无法进行章节切分”。

审核只读取文件前 8000 字符，章节切分只读取前 16000 字符，避免整篇文档都交给模型。

## 3. 新增学习智能体功能

### 3.1 课程知识点问答

接口：

- `POST /api/learning/knowledge/ask`

入参：

- `courseId`：课程 ID
- `question`：学生问题
- `conversationId`：可选，对话 ID；不传时后端自动生成

实现位置：

- `LearningController`
- `LearningServiceImpl#askCourseKnowledge`
- `LearningAiServiceImpl#askCourseQuestion`
- `CourseKnowledgeServiceImpl#searchCourseContext`

实现方式：

1. 学生选择课程并提问。
2. 后端读取课程下已上传文件对应的 Markdown COS 文件。
3. `CourseKnowledgeServiceImpl` 将 Markdown 切片，优先用 Spring AI `SimpleVectorStore` 做相似度检索。
4. `LearningAiServiceImpl` 使用 Spring AI `ChatClient` 基于资料片段回答。
5. 对话通过 `MessageChatMemoryAdvisor` 保存到 `chat_memory_message` 表。

如果 DashScope Embedding 不可用，系统会退回 Markdown 片段检索，保证问答接口可用。后续如果你要接持久化向量库，只需要替换 `CourseKnowledgeService` 这一层。

### 3.2 AI 出题考察

接口：

- `POST /api/learning/question/generate`

入参：

- `courseId`：课程 ID
- `questionCount`：题目数量，范围 1 到 10，默认 3
- `conversationId`：可选

实现方式：

1. 根据课程资料片段生成题目。
2. 使用 `ChatClient#entity(...)` 接收结构化结果。
3. 题目、标准答案、解析、类型、难度保存到 `learning_question` 表。
4. 返回给学生的 `LearningQuestionVO` 不暴露标准答案，避免提前泄露。
5. 出题交流信息保存到 `chat_memory_message` 表。

### 3.3 学生答题和错题

答题接口：

- `POST /api/learning/question/answer`

错题分页接口：

- `POST /api/learning/question/wrong/list/page`

实现方式：

1. 学生提交题目 ID 和答案。
2. 后端读取题目标准答案和解析。
3. 使用 Spring AI 判断学生答案是否正确。
4. 答题记录保存到 `learning_answer_record` 表。
5. 错题接口只查询当前学生答错的题目，并返回题目、学生答案、标准答案、解析和 AI 反馈。

### 3.4 学习动态画像

分析接口：

- `POST /api/learning/profile/analyze`

获取最近一次画像：

- `GET /api/learning/profile/get`

实现方式：

1. 后端根据用户 ID 查询 `chat_memory_message` 中的历史交流信息。
2. 同时查询该用户最近的错题记录。
3. 将学习历史交给 Spring AI 生成结构化画像。
4. 画像保存到 `learning_profile` 表。

画像字段包括：

- `knowledgeLevel`
- `learningStyle`
- `interest`
- `weakness`
- `errorPreference`
- `learningSpeed`
- `learningRoute`
- `behaviorAnalysis`

普通用户只能分析和查看自己的画像，管理员可以指定 `userId` 查看或分析其他用户画像。

## 4. 新增数据库脚本

新增 SQL 文件：

- `sql/learning.sql`

新增表：

- `learning_question`：保存 AI 为学生生成的题目。
- `learning_answer_record`：保存学生答题记录和 AI 判题反馈。
- `learning_profile`：保存学习动态画像、学习路线和行为分析。

项目已有表：

- `course_file`：保存课程原文件和 Markdown 文件 COS 地址。
- `chat_memory_message`：保存 Spring AI 对话记忆。

## 5. 新增代码结构

新增业务接口：

- `controller/LearningController.java`
- `service/LearningService.java`
- `service/impl/LearningServiceImpl.java`

新增 DTO：

- `model/dto/learning/LearningAskRequest.java`
- `model/dto/learning/LearningQuestionGenerateRequest.java`
- `model/dto/learning/LearningQuestionAnswerRequest.java`
- `model/dto/learning/LearningWrongQuestionQueryRequest.java`
- `model/dto/learning/LearningProfileAnalyzeRequest.java`

新增 VO：

- `model/vo/LearningAskVO.java`
- `model/vo/LearningQuestionVO.java`
- `model/vo/LearningAnswerVO.java`
- `model/vo/LearningWrongQuestionVO.java`
- `model/vo/LearningProfileVO.java`

新增实体和 Mapper：

- `LearningQuestion`
- `LearningAnswerRecord`
- `LearningProfile`
- `LearningQuestionMapper`
- `LearningAnswerRecordMapper`
- `LearningProfileMapper`

新增 AI 能力：

- `studyaiagent/course/service/CourseKnowledgeService.java`
- `studyaiagent/course/service/impl/CourseKnowledgeServiceImpl.java`
- `studyaiagent/learning/service/LearningAiService.java`
- `studyaiagent/learning/service/impl/LearningAiServiceImpl.java`
- `studyaiagent/learning/model/*`

## 6. 当前上线前还需要注意

1. 需要先执行 `sql/learning.sql` 创建新增表。
2. 当前课程知识库检索优先使用 Spring AI `SimpleVectorStore`，暂时是请求内临时向量库；后续建议替换为持久化向量库，避免每次请求重复 Embedding。
3. `chat_memory_message` 当前配合 `MessageWindowChatMemory` 保存窗口对话，适合问答上下文；如果要做长期全量学习分析，后续可以新增一张完整交流流水表。
4. AI 判题适合主观题和概念题，选择题、填空题后续可以增加后端规则判定，降低模型成本。
5. 画像分析目前是手动触发接口，后续可以做成定时任务或学生答题后异步刷新。

## 7. 测试结果

已执行：

```bash
.\mvnw.cmd test
```

结果：编译和现有 Spring Boot 测试通过。
