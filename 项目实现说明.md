# 项目实现说明

## 本次新增内容

### 1. 课程文件上传审核

- 在 `CourseServiceImpl#uploadCourseFile` 中完成原有两个 TODO。
- 上传课程文件前，会先把文件转换为 Markdown 文本。
- 使用 DashScope 大模型判断文件内容是否符合课程主题。
- 审核不通过时，直接拒绝上传，不会写入 COS 和数据库。
- 审核通过后，保存原始文件，同时保存一份 Markdown 文件到 COS。

### 2. 课程章节解析

- 用户选择章节解析时，会调用大模型从课程文件中提取大章节。
- 章节粒度只保留大章节，不做太细的知识点切分。
- 解析出的章节写入 `chapter` 表。
- 后续可以继续扩展章节排序、课程文件关联、知识点拆分。

### 3. AI 服务结构

- `studyaiagent/app`：应用编排层，负责组织学习智能体流程。
- `studyaiagent/course`：课程文件审核、Markdown 存储、章节解析相关能力。
- `studyaiagent/memory`：记忆能力。
- `ai/service`：通用大模型调用、文档转 Markdown 能力。
- `config`：DashScope 和后续 Spring AI 能力预留配置。

当前项目仍然是 Spring Boot 2.7，Spring AI Alibaba 最新 starter 依赖 Spring Boot 3.x，不适合直接硬接入。
本次先使用现有 DashScope SDK 封装成可复用服务，后续如果项目升级到 Spring Boot 3.x，可以在当前结构上替换为 Spring AI ChatClient。

### 4. 记忆持久化

- 新增数据库记忆表：`chat_memory_message`
- 保存字段包括：
  - 用户 id
  - 对话 id
  - 角色
  - 对话内容
  - 创建时间
- 新增文件记忆服务：
  - 默认保存到 `data/ai-memory`
  - `data/` 已加入 `.gitignore`

### 5. 本地配置

- 已创建 `src/main/resources/application-local.yml`
- 文件中放 COS 和 DashScope 本地测试配置
- 该文件已被 `.gitignore` 忽略，不会提交到 GitHub

## 数据库变更

### course_file 表

新增字段：

- `markdown_key`
- `markdown_url`

用于保存课程文件转换后的 Markdown 文件地址。

### chapter 表

用于保存课程章节。

### chat_memory_message 表

用于保存数据库持久化聊天记忆。

## 测试结果

已执行：

```bash
.\mvnw.cmd test
```

测试通过。

同时排除了 DashScope SDK 带入的 `slf4j-simple`，避免和 Spring Boot 默认 logback 产生重复日志绑定。
