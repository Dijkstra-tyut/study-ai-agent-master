# 当前项目实现与测试报告

## 一、当前已实现功能

### 1. 用户模块

- 用户注册：`POST /api/user/register`
- 用户登录：`POST /api/user/login`
- 获取当前登录用户：`GET /api/user/get/login`
- 用户退出登录：`POST /api/user/logout`
- 登录用户填写个人信息：`POST /api/user/profile/update`
- 登录用户查看个人信息：`GET /api/user/profile/get`

用户填写个人信息时，通过 `role` 区分学生和老师：

- `student`：写入 `student_profile` 表
- `teacher`：写入 `teacher_profile` 表

用户基础信息仍保存在 `user` 表，学生 / 老师扩展信息保存在对应资料表。

### 2. 管理员用户管理

- 创建用户：`POST /api/user/admin/add`
- 删除用户：`POST /api/user/admin/delete`
- 更新用户：`POST /api/user/admin/update`
- 分页查询用户：`POST /api/user/admin/list/page`
- 查询用户资料：`GET /api/user/admin/profile/get`
- 更新用户资料：`POST /api/user/admin/profile/update`

管理员接口通过 `@AuthCheck(mustRole = UserConstant.ADMIN_ROLE)` 做权限控制。

### 3. 文件模块

- 通用文件上传：`POST /api/file/upload`
- 头像上传：`POST /api/file/upload/avatar`
- 文件下载：`GET /api/file/download`
- 文件删除：`POST /api/file/delete`

文件上传通过 `FileUploadEnum` 区分业务目录：

- `avatar`：头像
- `course`：课程文件
- `knowledge`：知识库文件
- `text`：普通文本文件

文件真实内容上传到腾讯云 COS，数据库中保存文件名称、COS key、访问地址、类型、大小等信息。

本次已将通用文件上传、下载、删除接口加上登录校验，避免未登录用户直接通过 key 操作 COS 文件。

### 4. 教师课程模块

- 老师创建课程：`POST /api/teacher/course/add`
- 老师删除课程：`POST /api/teacher/course/delete`
- 老师更新课程：`POST /api/teacher/course/update`
- 老师分页查询自己的课程：`POST /api/teacher/course/list/page`
- 老师上传课程文件：`POST /api/teacher/course/file/upload`
- 老师删除课程文件：`POST /api/teacher/course/file/delete`
- 老师更新课程文件：`POST /api/teacher/course/file/update`
- 老师分页查询课程文件：`POST /api/teacher/course/file/list/page`
- 老师下载课程文件：`GET /api/teacher/course/file/download`

老师只能管理自己负责的课程和自己上传的课程文件。

### 5. 管理员课程模块

- 管理员创建课程：`POST /api/course/admin/add`
- 管理员删除课程：`POST /api/course/admin/delete`
- 管理员更新课程：`POST /api/course/admin/update`
- 管理员分页查询课程：`POST /api/course/admin/list/page`
- 管理员删除课程文件：`POST /api/course/admin/file/delete`
- 管理员更新课程文件：`POST /api/course/admin/file/update`
- 管理员分页查询课程文件：`POST /api/course/admin/file/list/page`
- 管理员下载课程文件：`GET /api/course/admin/file/download`

管理员可以管理全部课程和全部课程文件。

### 6. 学生课程模块

- 学生分页查看课程：`POST /api/student/course/list/page`
- 学生分页查看课程文件：`POST /api/student/course/file/list/page`
- 学生下载课程文件：`GET /api/student/course/file/download`

学生侧目前只实现查看和下载，不实现课程文件增删改。

## 二、本次修复的问题

### 1. 修复老师资料表映射错误

`Teacher` 实体原来没有指定 `@TableName("teacher_profile")`，MyBatis-Plus 会默认查询 `teacher` 表，导致填写老师资料、删除用户时报错。

本次已补充：

```java
@TableName(value ="teacher_profile")
```

### 2. 修复学生查看课程时被错误过滤的问题

原逻辑中，非管理员查询课程都会追加：

```java
teacher_id = 当前用户 id
```

这样学生查看课程时会被当成老师过滤，导致学生看不到正常课程。

本次改为：

- 管理员：查看全部
- 老师：只看自己的
- 学生：查看可见课程列表

### 3. 删除课程时同步删除 COS 文件

删除课程时，现在会先查出该课程下的 `course_file` 记录，并调用 `FileService.deleteFile` 删除 COS 上的真实文件，再删除数据库记录。

### 4. 课程文件上传失败时清理 COS 文件

课程文件上传流程是：

1. 上传文件到 COS
2. 保存 `course_file` 数据库记录

如果第二步保存数据库失败，现在会尝试删除刚上传到 COS 的文件，减少无效文件残留。

### 5. 增加事务控制

以下多步骤数据库操作已补充事务：

- 用户填写 / 更新个人信息
- 删除用户时同步删除学生 / 老师资料
- 删除课程
- 上传课程文件
- 删除课程文件

### 6. 文件接口增加登录保护

通用文件接口现在都需要登录：

- `/api/file/upload`
- `/api/file/download`
- `/api/file/delete`

头像上传本来就会通过 `userService.getLoginUser(request)` 获取登录用户。

### 7. 增加课程文件按 id 下载接口

新增按课程文件 id 下载的接口，避免前端直接依赖 COS key：

- 老师：`GET /api/teacher/course/file/download?id=文件id`
- 管理员：`GET /api/course/admin/file/download?id=文件id`
- 学生：`GET /api/student/course/file/download?id=文件id`

### 8. 修复 JSON 格式错误返回系统错误的问题

原来 JSON 解析失败会被 `RuntimeException` 捕获，返回 `50000 系统错误`。

现在对请求参数解析异常单独处理，返回：

```json
{
  "code": 40000,
  "message": "请求参数错误"
}
```

### 9. SQL 脚本可重复执行

`sql/AllUsers.sql` 和 `sql/course.sql` 的建表语句已改成：

```sql
CREATE TABLE IF NOT EXISTS
```

重复执行脚本不会因为表已存在而失败。

### 10. 本地配置不提交 Git

已忽略：

```gitignore
src/main/resources/application-local.yml
```

本地数据库密码和 COS 密钥只放在本机 `application-local.yml` 中，不上传到 GitHub。

## 三、COS 配置测试结果

本地使用以下配置格式可以被 Spring Boot 正常读取：

```yaml
cos:
  client:
    host: https://你的桶域名
    secret-id: 你的 secretId
    secret-key: 你的 secretKey
    region: ap-shanghai
    bucket: 你的 bucket
```

注意：建议使用 `secret-id`、`secret-key` 这种写法，和 `CosClientConfig` 中的条件配置完全对应。

本次测试中，COS 客户端已成功创建，并且已经向腾讯云 COS 发起请求。失败原因不是代码没有读取配置，而是腾讯云返回：

```text
InvalidAccessKeyId
The Access Key Id you provided does not exist in our records
```

说明当前提供的 `secretId` 在腾讯云侧不被识别。常见原因：

- secretId 复制错误
- 该密钥已经被删除或禁用
- 不是当前腾讯云账号下的访问密钥
- 密钥不是 CAM 访问密钥
- 密钥泄露后已被安全策略禁用

建议在腾讯云控制台重新生成一组 CAM 访问密钥，并授予对应 COS bucket 的上传、下载、删除权限。

## 四、本次测试结果

已通过：

- `mvnw.cmd test`
- Spring Boot 本地启动
- Swagger 接口文档生成
- 用户注册
- 用户登录
- 老师填写个人信息
- 学生填写个人信息
- 老师创建课程
- 老师分页查询自己的课程
- 学生分页查询课程
- 管理员更新课程
- 管理员分页查询课程
- 管理员创建用户
- 管理员更新用户
- 管理员分页查询用户
- 管理员删除用户
- JSON 错误请求返回参数错误
- 未登录访问文件上传 / 下载接口返回未登录

未通过：

- COS 实际上传文件

原因是腾讯云返回 `InvalidAccessKeyId`，需要更换正确的腾讯云 COS 访问密钥后再测。

## 五、仍然保留的 TODO

课程文件上传后，目前只把 `review_status` 设置为 `pending`。

大模型审核逻辑仍然保留 TODO，没有实现：

```java
// TODO 接入大模型后，在这里根据课程主题审核文件内容，并更新 review_status。
```

等后续确定大模型接入方式后，可以在课程文件上传后增加审核服务。
