# 当前项目实现与测试报告

## 一、用户功能

已实现：

- 用户注册：`POST /api/user/register`
- 用户登录：`POST /api/user/login`
- 获取当前登录用户：`GET /api/user/get/login`
- 用户退出登录：`POST /api/user/logout`
- 登录用户填写个人信息：`POST /api/user/profile/update`
- 登录用户查看个人信息：`GET /api/user/profile/get`
- 管理员创建用户：`POST /api/user/admin/add`
- 管理员删除用户：`POST /api/user/admin/delete`
- 管理员更新用户：`POST /api/user/admin/update`
- 管理员分页获取用户列表：`POST /api/user/admin/list/page`
- 管理员查看用户资料：`GET /api/user/admin/profile/get`
- 管理员更新用户资料：`POST /api/user/admin/profile/update`

用户个人信息通过 `role` 区分学生和老师：

- `student`：写入 `student_profile`
- `teacher`：写入 `teacher_profile`

## 二、课程和课程文件功能

课程和课程文件接口已统一到 `CourseController`，避免管理员、老师、学生各写一套重复接口。

### 老师和管理员可以操作

管理员在 AOP 中拥有所有角色权限，因此可以直接使用这些老师权限接口。

- 创建课程：`POST /api/course/add`
- 删除课程：`POST /api/course/delete`
- 更新课程：`POST /api/course/update`
- 上传课程文件：`POST /api/course/file/upload`
- 删除课程文件：`POST /api/course/file/delete`
- 更新课程文件：`POST /api/course/file/update`

老师只能操作自己负责的课程和课程文件；管理员不受这个归属限制。

### 登录用户可以查看和下载

- 分页查看课程：`POST /api/course/list/page`
- 分页查看课程文件：`POST /api/course/file/list/page`
- 下载课程文件：`GET /api/course/file/download?id=课程文件id`

这些接口只要求登录，不要求老师或管理员角色。

## 三、文件功能

通用文件接口保留在 `FileController`：

- 通用文件上传：`POST /api/file/upload`
- 头像上传：`POST /api/file/upload/avatar`
- 通用文件下载：`GET /api/file/download`
- 通用文件删除：`POST /api/file/delete`

当前做了边界限制：

- `biz=course` 不允许走通用上传接口，课程文件必须走 `/api/course/file/upload`
- `course/` 目录下的文件不允许走通用下载、删除接口，必须走课程文件 id 接口
- 通用上传、删除要求老师或管理员权限
- 头像上传、通用下载只要求登录

## 四、返回对象脱敏

课程和课程文件展示给登录用户时，已经使用 VO：

- `CourseVO`
- `CourseFileVO`

`CourseFileVO` 不返回 `file_key`，也不直接返回 COS 地址，用户下载文件统一通过：

```text
GET /api/course/file/download?id=课程文件id
```

这样前端学生侧不会直接拿到 COS key。

## 五、审核字段处理

已删除课程文件中的 `review_status` 审核字段：

- `course_file` 建表 SQL 中已删除
- `CourseFile` 实体中已删除
- `CourseFileUpdateRequest` 中已删除
- `CourseServiceImpl` 中不再设置或更新审核状态

后续大模型审核不再通过审核字段流转。预留位置在课程文件上传处：

```java
// TODO 接入大模型后，先判断课程文件是否符合课程主题，不符合直接拒绝上传；符合后解析文件章节并写入章节表。
```

也就是说，大模型判断不合规时直接拒绝上传；合规后再进行章节解析。

## 六、本次测试

已执行：

```bash
.\mvnw.cmd test
```

结果：通过。

目前仍有 SLF4J 多绑定警告，来自新增依赖中的 `slf4j-simple` 和项目现有 `logback-classic` 同时存在，不影响当前编译和测试。
