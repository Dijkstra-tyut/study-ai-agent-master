# 接口变更说明

## 本次实现内容

1. 用户登录后可以填写个人信息，选择 `student` 或 `teacher` 后分别写入 `student_profile` 或 `teacher_profile` 表。
2. 管理员可以创建用户、删除用户、更新用户、分页查询用户列表。
3. 管理员可以查询和维护指定用户的个人信息。
4. 文件接口接入已有的 `CosManager`，支持普通文件上传、头像上传、文件下载、文件删除。
5. COS 配置不完整时项目可以正常启动，真正调用文件接口时会返回 `COS配置未完成`。

## 用户个人信息接口

### 填写或更新当前登录用户信息

`POST /api/user/profile/update`

学生示例：

```json
{
  "role": "student",
  "avatar": "https://xxx/avatar.png",
  "major": "计算机科学与技术",
  "grade": "2023级",
  "learning_target": "学习 Java 后端开发",
  "interest_direction": "AI 应用开发",
  "knowledge_level": "Java 基础，了解 MySQL"
}
```

教师示例：

```json
{
  "role": "teacher",
  "avatar": "https://xxx/avatar.png",
  "teacher_name": "张老师",
  "research_area": "人工智能",
  "intro": "负责 AI 课程教学"
}
```

### 获取当前登录用户信息

`GET /api/user/profile/get`

## 管理员接口

管理员接口都需要当前登录用户角色为 `admin`。

### 创建用户

`POST /api/user/admin/add`

```json
{
  "username": "testuser",
  "password": "12345678",
  "role": "student",
  "avatar": "https://xxx/avatar.png"
}
```

### 删除用户

`POST /api/user/admin/delete`

```json
{
  "id": 1
}
```

删除用户时会同步删除对应的学生信息和教师信息。

### 更新用户

`POST /api/user/admin/update`

```json
{
  "id": 1,
  "username": "newname",
  "password": "12345678",
  "role": "teacher",
  "avatar": "https://xxx/avatar.png"
}
```

### 分页查询用户

`POST /api/user/admin/list/page`

```json
{
  "current": 1,
  "pageSize": 10,
  "username": "test",
  "role": "student"
}
```

### 查询指定用户个人信息

`GET /api/user/admin/profile/get?userId=1`

### 更新指定用户个人信息

`POST /api/user/admin/profile/update`

请求体和 `/api/user/profile/update` 相同，但需要额外传 `userId`。

## 文件接口

### 普通文件上传

`POST /api/file/upload`

表单字段：

- `file`：文件
- `biz`：上传业务目录，可选值见 `FileUploadEnum`

当前内置目录：

- `avatar`：头像
- `course`：课程文件
- `knowledge`：知识库文件
- `text`：文本

返回：文件名、COS key、访问 URL、文件类型、文件大小。如果没有配置 `cos.client.host`，`fileUrl` 返回 COS key。

### 头像上传

`POST /api/file/upload/avatar`

表单字段：`file`

要求当前用户已登录。上传成功后会同步更新当前用户的 `avatar` 字段。

支持格式：`jpg`、`jpeg`、`png`、`webp`。

### 文件下载

`GET /api/file/download?key=file/xxx.png`

这里的 `key` 是 COS 对象 key，不是完整 URL。

### 文件删除

`POST /api/file/delete?key=file/xxx.png`

## COS 配置

需要在配置文件中补充：

```yaml
cos:
  client:
    host: https://你的访问域名
    secret-id: 你的 secretId
    secret-key: 你的 secretKey
    region: ap-xxx
    bucket: bucket-name-数字
```

如果这些配置没有填写，项目仍然可以启动，但调用上传、下载、删除接口会失败并提示 `COS配置未完成`。

## 验证结果

已执行：

```bash
.\mvnw.cmd test
```

结果：通过。

## 课程接口

老师接口需要当前登录用户角色为 `teacher`。老师只能操作自己负责的课程和自己上传的课程文件。

管理员接口需要当前登录用户角色为 `admin`。管理员可以操作全部课程和课程文件。

### 老师创建课程

`POST /api/teacher/course/add`

```json
{
  "course_name": "Java 后端开发",
  "description": "Spring Boot 基础课程"
}
```

### 老师删除课程

`POST /api/teacher/course/delete`

```json
{
  "id": 1
}
```

### 老师更新课程

`POST /api/teacher/course/update`

```json
{
  "course_id": 1,
  "course_name": "Java 后端开发进阶",
  "description": "更新后的课程介绍"
}
```

### 老师分页查询课程

`POST /api/teacher/course/list/page`

```json
{
  "current": 1,
  "pageSize": 10,
  "course_name": "Java"
}
```

### 老师上传课程文件

`POST /api/teacher/course/file/upload?courseId=1`

表单字段：`file`

课程文件会上传到 COS 的 `course/` 目录，并写入 `course_file` 表。

### 老师管理课程文件

- 删除：`POST /api/teacher/course/file/delete`
- 更新：`POST /api/teacher/course/file/update`
- 分页查询：`POST /api/teacher/course/file/list/page`

### 管理员课程接口

- 创建课程：`POST /api/course/admin/add`
- 删除课程：`POST /api/course/admin/delete`
- 更新课程：`POST /api/course/admin/update`
- 分页查询课程：`POST /api/course/admin/list/page`
- 删除课程文件：`POST /api/course/admin/file/delete`
- 更新课程文件：`POST /api/course/admin/file/update`
- 分页查询课程文件：`POST /api/course/admin/file/list/page`

## 大模型审核预留

已在两个地方预留 TODO：

- `FileManager.validFile`：上传文件基础校验后，可在这里接入大模型做通用文件内容审核。
- `CourseServiceImpl.uploadCourseFile`：课程文件入库前后，可在这里根据课程主题做课程资料审核，并更新 `review_status`。
