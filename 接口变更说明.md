# 接口变更说明

## 本次实现内容

1. 用户登录后可以填写个人信息，选择 `student` 或 `teacher` 后分别写入 `student_profile` 或 `teacher_profile` 表。
2. 管理员可以创建用户、删除用户、更新用户、分页查询用户列表。
3. 管理员可以查询和维护指定用户的个人信息。
4. 文件接口接入已有的 `CosManager`，支持普通文件上传、头像上传、文件下载、文件删除。
5. COS 配置不完整时项目可以正常启动，真正调用文件接口时会返回 `COS配置未完成`。

## 用户个人信息接口

### 填写或更新当前登录用户信息

`POST /api/user/profile/update`

学生示例：

```json
{
  "role": "student",
  "avatar": "https://xxx/avatar.png",
  "major": "计算机科学与技术",
  "grade": "2023级",
  "learning_target": "学习 Java 后端开发",
  "interest_direction": "AI 应用开发",
  "knowledge_level": "Java 基础，了解 MySQL"
}
```

教师示例：

```json
{
  "role": "teacher",
  "avatar": "https://xxx/avatar.png",
  "teacher_name": "张老师",
  "research_area": "人工智能",
  "intro": "负责 AI 课程教学"
}
```

### 获取当前登录用户信息

`GET /api/user/profile/get`

## 管理员接口

管理员接口都需要当前登录用户角色为 `admin`。

### 创建用户

`POST /api/user/admin/add`

```json
{
  "username": "testuser",
  "password": "12345678",
  "role": "student",
  "avatar": "https://xxx/avatar.png"
}
```

### 删除用户

`POST /api/user/admin/delete`

```json
{
  "id": 1
}
```

删除用户时会同步删除对应的学生信息和教师信息。

### 更新用户

`POST /api/user/admin/update`

```json
{
  "id": 1,
  "username": "newname",
  "password": "12345678",
  "role": "teacher",
  "avatar": "https://xxx/avatar.png"
}
```

### 分页查询用户

`POST /api/user/admin/list/page`

```json
{
  "current": 1,
  "pageSize": 10,
  "username": "test",
  "role": "student"
}
```

### 查询指定用户个人信息

`GET /api/user/admin/profile/get?userId=1`

### 更新指定用户个人信息

`POST /api/user/admin/profile/update`

请求体和 `/api/user/profile/update` 相同，但需要额外传 `userId`。

## 文件接口

### 普通文件上传

`POST /api/file/upload`

表单字段：`file`

返回：文件访问 URL。如果没有配置 `cos.client.host`，返回 COS key。

### 头像上传

`POST /api/file/upload/avatar`

表单字段：`file`

要求当前用户已登录。上传成功后会同步更新当前用户的 `avatar` 字段。

支持格式：`jpg`、`jpeg`、`png`、`webp`。

### 文件下载

`GET /api/file/download?key=file/xxx.png`

这里的 `key` 是 COS 对象 key，不是完整 URL。

### 文件删除

`POST /api/file/delete?key=file/xxx.png`

## COS 配置

需要在配置文件中补充：

```yaml
cos:
  client:
    host: https://你的访问域名
    secret-id: 你的 secretId
    secret-key: 你的 secretKey
    region: ap-xxx
    bucket: bucket-name-数字
```

如果这些配置没有填写，项目仍然可以启动，但调用上传、下载、删除接口会失败并提示 `COS配置未完成`。

## 验证结果

已执行：

```bash
.\mvnw.cmd test
```

结果：通过。
